using System;
// pick which to uncomment
namespace while_test
{
    class Program
    {
        static void Main(string[] args)
        {
            /* while guess tool */
            // int counter;
            // while(true)
            // {
            //     Console.WriteLine("Enter a number between 1-10");
            //     counter = int.Parse(Console.ReadLine());
            //     if (counter >=1 && counter <=10)
            //     {
            //         if (counter == 5)
            //         {
            //             Console.WriteLine("Correct Answer");
            //             break;
            //         } else {
            //             Console.WriteLine("Try again");
            //         }
            //     }
            // }

            /* while counter */
            // int count = 1;
            // while(count <=10)
            // {
            //     Console.WriteLine(count);
            //     count++;
            // }
            
            /* do while loop */
            // int count = 1;
            // do
            // {
            //     Console.WriteLine(count);
            //     count++;
            // } while(count <= 10);

        /* for loop */
        // int total = 0;
        // int[] items_to_store;
        // items_to_store = new int[5] {10,20,30,40,50};
        // for(int count = 0; count <= 4; count++)
        // {
        //     total = total + items_to_store[count];
        //     Console.WriteLine(String.Format("Iteration count {0}, Element {1}, Total {2}", 
        //     count, items_to_store[count], total));
        // }

        }
    }
}
